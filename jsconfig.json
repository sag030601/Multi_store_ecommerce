{
  "compilerOptions": {
    "baseUrl": ".",  // Set the base directory as the root of your project
    "paths": {
      "@/*": ["./src/*"]  // This tells Next.js/JS to resolve '@/...' as './src/...'
    }
  }
}
